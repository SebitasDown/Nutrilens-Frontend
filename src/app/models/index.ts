/**
 * Interfaces, tipos y DTOs compartidos.
 * Modelos de dominio y contratos de API.
 * Ej: User, Meal, NutritionReport.
 */

export interface AppConfig {
  production: boolean;
  apiUrl: string;
}
